# 参与贡献

本仓库仍以规格和设计文档为主。提交实现前，请先确认对应事项已经明确范围、验收条件和依赖。

## 交流语言

中文是项目文档和正式事项的主要语言。GitHub Issue 与 Pull Request 同时接受中文和英文；英文反馈被采纳后，维护者会在对应的 `.scratch/` 票据中记录中文摘要。

## 提交问题或建议

1. 先搜索已有 GitHub Issue 和 `.scratch/windows-initial-setup-workbench/issues/`。
2. 没有对应内容时，可以创建空白 GitHub Issue，说明具体场景、期望行为、已知限制和参考资料。
3. GitHub Issue 是公开反馈入口。确认处理后，由维护者建立或关联 `.scratch/` 票据；规格、范围和实施状态以该票据为准。

不要自行重编号既有票据。只有在维护者明确要求时，Pull Request 才直接新增或调整 `.scratch/` 事项。

## 提交 Pull Request

- 每个 Pull Request 聚焦一个可审查的目标，并链接相关 GitHub Issue 或 `.scratch/` 票据。
- 修改规格时同步核对 `CONTEXT.md`、相关 ADR 和受影响事项，避免同一规则出现相互矛盾的版本。
- 修改实现或模块边界时，说明受影响的模块、接口、状态所有者和依赖方向，并遵守 [架构与代码质量](docs/engineering/architecture-and-code-quality.md)；界面不得直接执行系统操作，也不得用全局可变状态、服务定位器或万能工具模块绕过边界。
- 凡新增普通软件、来源、设置、驱动入口或优化方案能由既有内置受控能力表达，就只能增加经过校验的目录数据；新操作类型必须作为项目维护并随工作台编译发布的受控能力扩展接入，不接受第三方可执行插件。
- 修改 WinUI 3 界面或交互动效时，说明动效服务的目的、操作频率、输入方式、可中断与反向操作、减少动画回退，并遵守 [WinUI 3 Apple 风格设计准则](docs/design/winui3-apple-inspired.md)。
- 如实列出已经执行、未执行和无法执行的验证；文档检查、源码检查和真实设备验收不能互相替代。
- 实现类 Pull Request 至少列出无界面核心检查、受影响接口的合同测试和架构检查结果；界面类 Pull Request 还需列出键盘、鼠标、触摸、减少动画及快速重复操作的检查结果。
- 不提交凭据、个人绝对路径、本地索引、生成缓存或 GitNexus 生成的 agent 文档与技能。
- 不复制许可不明的第三方代码或文档。第三方二进制资源是否可随包、进入项目备用源或公开 Release 由 [ADR-0036](docs/adr/0036-candidate-resources-may-be-bundled-and-released.md) 决定；提交必须如实保留已知许可、来源、身份和再分发结论，不能把证据缺失表述为已获权、已验证或项目背书。

## 维护软件目录

项目维护者直接修改权威的 [`catalog/software-catalog.toml`](catalog/software-catalog.toml)，并按[软件目录维护指南](docs/maintainers/software-catalog-input.md)填写软件分类、版本规则、来源用途、任意受支持形式的 HTTP(S) 原始地址、项目 GitHub 备用包安排和用户说明。维护者不选择“官网页面”“直链”或“GitHub Releases”等技术类型，也不追踪会变化的最终下载地址。

工作台直接校验和读取 TOML，自动识别直链、跳转、普通网页、GitHub 或托管来源，并由项目内置受控能力补足解析和安装事实；不再维护开发者手工转换的第二份目录数据。维护者也可以在调试模式中使用图形化编辑器维护同一 TOML 兼容模型，但编辑器仍调用相同核心校验，不能建立界面私有目录。编辑器中的“保存草稿”不会更新当前软件列表；已经保存但尚未应用的草稿跨正常关闭和重新打开保留，仍显示“已保存未应用”，直至维护者继续编辑、明确应用或删除。同一次运行中若存在未保存修改，切换页面、切换标准视图与高级视图或关闭调试模式都不弹关闭确认，也不自动保存、放弃或应用；重新打开编辑器后仍显示“未保存更改”。只有主动关闭整个工作台时，才会要求保存草稿并关闭、仅放弃未保存修改或返回编辑，任何选择都不会应用目录。异常退出或电脑断电后，工作台尽量找回未保存内容并标为“恢复的未保存修改”；它不会覆盖此前草稿或当前目录，仍须先明确保存才能应用，放弃也只移除恢复部分。只有维护者明确点击“应用”并通过运行时加载校验，才完整切换当前有效目录，既有批次继续使用原快照。正式发布门禁未通过但运行时可加载、且 `release_state = "draft"` 的内容可以明确应用或导入为本机试用目录；这不会修改发布状态，也不能成为合入发行制品的依据。新增普通软件和来源不得要求在 C++ 中逐项硬编码，也不得把脚本、解析代码或第三方可执行插件放入目录。

维护中的目录保持 `release_state = "draft"`。只有全部首版必达条目均已启用，依赖、来源版本和受控安装档案通过发布级校验，并递增 `revision` 后，才可改为 `release` 并收入发行制品。

## 贡献许可

提交 Issue 或 Pull Request 即表示你有权提供其中内容，并同意被项目采纳的贡献可以按本仓库的 [MIT License](LICENSE) 发布。项目当前不要求签署 CLA，也不采用 DCO。
