# 为未来 macOS 保留可移植 C++ 核心

Status: accepted

首版继续使用 WinUI 3 构建 Windows 界面，未来 macOS 版接受按平台习惯重新制作界面，只复用可移植的 C++ 核心规则和通用数据契约。Windows 设置、硬件、进程、权限与重启等能力通过核心拥有的接口和平台适配器接入，平台差异不能以散布条件判断的方式污染可移植模块；macOS 不属于首版发行和测试范围，也不为此提前建设共享界面框架或假想适配层。
