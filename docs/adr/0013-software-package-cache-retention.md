# 设置软件包缓存保留期限

Status: accepted

便携版和安装版共享工作台下载的软件包缓存，其中可以包含官方完整安装包、仍需联网的在线安装器和项目离线软件包。缓存默认保留 7 天，并允许用户改成立即删除、30 天或不自动删除，同时可选择本机、网络共享和可移动位置；相同软件、版本、架构和来源可以复用，在线安装器被缓存后仍不得标记为离线可用。该策略在重复下载、离线恢复和磁盘占用之间取平衡，具体清理与失败行为由首版规格定义。
