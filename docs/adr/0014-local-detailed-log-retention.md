# 永久保留详细本机执行日志

执行日志只保存在本机、默认永久保留且不自动上传，用户可主动导出或清空。调试模式的诊断导出形成一个格式版本可识别、自包含且已经脱敏的文件，携带工作台可观察到的构建与环境摘要、相关目录和批次上下文、事件链、原始错误、最后可信状态及明确的采集缺口，使维护者无需访问原测试机或另取截图和环境清单即可定位问题。日志与支持设置撤销和跨重启恢复的恢复记录分离，清空日志不破坏恢复能力。该决定优先保证首装问题可以异机详细追溯，同时把诊断资料的控制权留给用户；代价是日志合同、脱敏和格式版本识别必须作为长期维护能力。
