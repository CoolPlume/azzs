# 在同一应用内分离 WinUI 3 界面与 C++ 核心

Status: accepted

Windows 版采用 C++/WinRT 与 XAML 构建 WinUI 3 界面，并使用 C++26 初始化核心；界面只依赖不暴露 WinUI 类型的核心接口，核心拥有业务状态和结果语义，界面不直接操作系统能力或保存第二套业务规则。具体适配器只在唯一装配入口连接。两层仍属于同一个常驻管理员桌面应用，不拆成独立程序、后台服务或本地网络通信，以保持离线运行、关闭即暂停和状态恢复语义简单一致。
