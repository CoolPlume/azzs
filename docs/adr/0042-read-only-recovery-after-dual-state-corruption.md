# 双代状态损坏时进入状态损坏恢复模式

Status: accepted
Date: 2026-08-10
Related: ADR-0029

当前权威状态与最后可信代次都无法通过校验时，工作台进入显式不可修改的状态损坏恢复模式，不得静默创建空状态或继续发起初始化操作。工作台保留原始字节与诊断证据，只提供诊断导出、读取仍可恢复的历史，以及在用户看清逐类数据影响后明确确认的重新初始化；重新初始化形成新代次，不能把损坏内容伪报为已恢复。

该决定优先保护设备状态与审计事实，代价是用户必须处理恢复出口后才能继续修改设备。
