# 基础设施适配器边界

事项 02 在应用核心先定义状态、占用、日志和时钟接口后，于此提供本机文件日志、
状态支持的设备操作占用、结构化日志与系统时钟适配器。Windows ProgramData、ACL、
交互主体和状态文件原语仍位于 `adapters/windows`，并只由 Windows 唯一装配入口组合。

无系统副作用的内存文件系统、固定时钟、日志存储、故障注入和占用存储位于
`tests/support/`，只供独立合同测试使用，不被生产宿主链接。
