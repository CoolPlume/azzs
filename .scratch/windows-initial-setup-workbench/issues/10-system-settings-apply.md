# 实现系统优化应用与验证

Type: task  
Status: completed  
Resolution: completed
Blocked by: 02, 04, 24
Owner: issue-10
Claimed by: codex/issue-10-system-settings-apply-v2
Consumers: 11, 12, 16, 20, 21
Verification: 以测试注册表和资源管理器适配器覆盖计划、原值、串行执行、验证、失败和重启语义。
Evidence freshness: 绑定当前提交、设置目录修订和声明的 Windows 适用范围。

## Goal

安全执行精选目录中的系统优化单项或推荐总体优化，记录原值并验证结果。

## Acceptance Criteria

- [x] 只执行当前有效系统设置目录中的受控设置项，不接受用户输入的脚本、命令、注册表内容或目标路径。
- [x] “推荐总体优化”只是同一批设置项的可调整组合；无论从总体方案还是单项入口执行，都共享同一状态、历史、验证与撤销语义。
- [x] 总体方案在执行前显示摘要并允许展开、勾选或取消各设置；低影响项目可以默认选择，明显改变使用习惯或界面且影响较大的项目默认不选。
- [x] 不按当前用户、软件账号或整台电脑给设置分类或决定是否收录，只用通俗文案说明实际会影响什么。
- [x] 执行前展示目标效果、当前状态、说明来源、已知适用版本、资源管理器或 Windows 重启要求和撤销能力。
- [x] 已符合目标状态的项目显示“已生效”并跳过重复修改。
- [x] 执行前保存可逆设置原值，并按依赖关系确定执行顺序。
- [x] 系统优化按确定顺序串行执行；当前项的系统修改、原值记录、验证和相关权威提交完成前不得启动下一项，也不得与安装或软件优化的实际修改并行。无副作用 CPU 密集型辅助工作不能绕过此边界。
- [x] 执行时冻结设置稳定标识、目录版本、目标、验证、撤销和重启语义。
- [x] 执行后验证工作台发起的变更并显示成功、失败或等待重启。
- [x] 单项失败不终止没有依赖关系的后续设置，结果按实际设置逐项显示，不以总体方案名称掩盖部分失败。
- [x] 对超出目录已知适用 Windows 范围的设置，标准视图显示“可能不适用”并禁用普通执行；高级视图仅在具备恢复方案时提供“仍然尝试”，单独说明风险并记录确认。
- [x] 支持目录预定义的“Windows 11 经典右键菜单”和“Windows 10 风格资源管理器”设置直接应用或恢复 Windows 11 默认状态；每次修改前均保存当时状态和恢复数据。
- [x] 上述两项生效需要重启资源管理器时，提供“立即重启资源管理器”和“稍后处理”；立即重启前说明任务栏与已打开的资源管理器窗口会短暂关闭，稍后处理则保存“等待资源管理器重启”状态。
- [x] 资源管理器或 Windows 后续重启、以及工作台重新打开后会重新检测上述两项的实际状态，未验证前不宣称已经生效。

## References

`SET-01` 至 `SET-08`、`SET-20` 至 `SET-35`、`TECH-33` 至 `TECH-34`

## Comments

- 最终集成：PR #19 已合入 `codex/v1-integration`，提交 `ebcbf9346a2400314760201e71ac5bac5b70443a`。
- 验证：合并前的精确 head `1e140e7eae457b96d09d992524a5443ce5bec3b8` 通过 Windows x64 与 ARM64 CI（run `31597847926`）；合并后受影响的 host 构建及 `system-settings-apply`、`application-update` 合同测试通过。
- 已按要求执行过一次 `cmake --workflow --preset host-guardrails`，未重复运行既已通过的完整门禁。
- 未进行真实 Windows 实机验证；Windows 映射由受控类型边界、合同测试与 GitHub Windows CI 覆盖，但不代表真实资源管理器重启、UAC 或 UI 交互已验证。
