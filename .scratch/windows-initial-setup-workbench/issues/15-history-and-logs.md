# 实现历史与日志页面

Type: task  
Status: ready-for-agent  
Resolution: completed
Blocked by: 02, 08, 11, 12, 24, 27, 28
Owner: issue-15
Consumers: 16, 18, 21, 32
Verification: 历史投影、不可变重试、日志分段、脱敏与单文件诊断 golden 和故障注入测试。
Evidence freshness: 绑定功能提交 `f91fd9684f99d3080ef49b50e8098c2e8039cb8b`、合入提交 `a3c5f837afd8f1674026a62a05bd1cd43039d8df`、事件与导出模式和输入日志；产品、目录、事件模式或诊断导出格式改变后须重验。实机日志只证明其记录的构建与环境。

## Goal

让用户查看不可变执行历史、逐项状态和详细本机日志，并能导出诊断资料。

## Ownership Boundary

本事项拥有历史、日志和诊断资料的读取投影、查看、定位、清空日志命令及单文件诊断导出体验；设备级事件与保留边界由事项 02 拥有，调试模式及其当前状态由事项 32 拥有，设置页只由事项 18 承载入口。本事项不保存另一份日志粒度、调试状态、目录草稿或当前有效目录。

## Acceptance Criteria

- [ ] 分别展示推荐流程记录、软件安装批次、软件优化批次、系统优化操作、取消记录及目录、来源、方案和版本快照。
- [ ] 系统优化历史区分“推荐总体优化”选择来源与单项选择，最终始终列出实际执行的设置及逐项结果、原值恢复记录和撤销结果。
- [ ] 对带 Windows 适用范围的设置，历史与日志保存实际 Windows 版本及内部版本号、目录声明的已知范围、超范围尝试确认、资源管理器重启选择与验证结果。
- [ ] 历史逐项显示规格定义的统一状态和必要原因。
- [ ] 已取消批次、下架设置和目录移除项保留只读历史。
- [ ] 重试创建新记录，不覆盖原记录。
- [ ] 自动检测确认与用户确认的安装和优化结果均可区分；安装器交互还区分受控自动处置、工作台交互确认和官方界面处理，记录预期选择、可观察结果、用户确认和回退原因，且不与安装结果待确认混用。账户与个人资料步骤只记录官方界面交接和可观察结果，不记录其输入内容。安装结果待确认还记录已观察到的完成事实和安装后置行为未知原因，两类“结果待确认”的用户选择作为不可改写记录保留。
- [ ] 来源手动交接作为软件选择或推荐流程记录，保留软件稳定标识、原解析失败原因、打开的原始地址、交接时间、每次重新检测的时间和结果、用户选择“暂时跳过”的时间、当前是否仍列入未完成列表，以及推荐流程中用户点击“继续”的时间；后来识别成功只更新当前状态，不删除既有交接历史。它不创建安装批次，“外部安装已识别”与工作台完成的安装使用不同状态，日志不虚构未观察到的下载或安装过程。
- [ ] 优化历史完整展示强制执行、强制关闭、强制终止、停止、取消、重启交接、自动或用户确认、失败、普通下架和紧急撤回，且可由软件优化页的最近结果直接定位。
- [ ] 正常关闭暂停与异常退出分别记录；异常退出记录最后已持久化步骤、结果未知状态和恢复后的重新检测结果。
- [ ] 支持查看、导出和清空详细日志。
- [ ] 调试日志视图消费事项 02 的结构化事件和事项 32 的调试状态投影，能按命令、状态转换、目录、来源、下载、安装程序、恢复、适配器结果、耗时、重试和关联标识读取与定位；本事项不决定或持久化当前日志粒度。
- [ ] 查看与导出路径保持写入端已经执行的集中脱敏，不重新引入口令、令牌、Cookie、授权头、计算机名、用户名、唯一设备标识、MAC/IP、SSID、用户目录名称或地址与路径中的凭据；设置页入口由事项 18 承载，关闭调试模式不会使本事项删除既有日志。
- [ ] 一次诊断导出形成一个版本化、自包含且已脱敏的文件；未接触测试机的开发者只查看该文件即可确认工作台构建、Windows 与相关硬件驱动环境、当前有效目录的模式、修订、`release_state`、本机试用标记、最近目录应用关联标识与加载和发布结果、相关批次冻结的目录身份、用户命令与状态顺序、失败组件和阶段、原始错误、重试及最后可信状态，不依赖远程访问、截图、口述步骤或另一份环境清单。
- [ ] 导出文件明确标记调试覆盖范围、事件序号、最后落盘点、异常结束、所有已知或可检测的丢弃、截断、刷新失败、平台未提供或脱敏缺口，以及最后落盘后的未知区间；采集不完整时仍可导出，但不能把缺失事实显示成正常或完整。
- [ ] 清空日志不影响来源手动交接记录、恢复记录、安装或优化历史、已生效设置、已完成优化、待恢复流程或有效紧急撤回信息。
- [ ] 日志、历史和恢复记录使用独立聚合；并发清空先提交截止事件并切换到新日志段，空间不足时停止非关键日志并记录可检测缺口，绝不自动删除历史或恢复记录。

## References

`HIST-01` 至 `HIST-07`、`LOG-01` 至 `LOG-13`、`DBG-01` 至 `DBG-03`、`DBG-07`、`OPT-51`、`OPT-56`、`OPT-57`、规格第 15.3 节

## Answer

- 功能分支 `codex/issue-15-history-and-log-completion` 的功能提交 `f91fd9684f99d3080ef49b50e8098c2e8039cb8b` 已由 PR [#48](https://github.com/CoolPlume/azzs/pull/48) 普通合入 `codex/v1-integration`，产品合入提交为 `a3c5f837afd8f1674026a62a05bd1cd43039d8df`。本票据证据提交不重复执行已绑定到该功能提交的构建、合同或 CI。
- Windows x64 focused `history-and-logs.contract` 已在功能候选通过。GitHub Actions run [31830571268](https://github.com/CoolPlume/azzs/actions/runs/31830571268) 的 [x64 Release job 94865001202](https://github.com/CoolPlume/azzs/actions/runs/31830571268/job/94865001202) 为 `success`，绑定同一功能提交。
- 既有 Windows host guardrails 记录为环境边界，未为本证据提交重跑：完整性 ACL root 夹具缺失使 `execution-log.contract` 不能在该主机完成；目录符号链接 root 夹具缺失使 `windows-device-data.contract` 不能在该主机完成。这两项不是历史/日志产品通过的声明，也没有通过修改产品或夹具掩盖该环境前置条件。
- ARM64 未在本机执行，按当前 x64 优先策略延期；本票不把自动 CI 的 ARM64 job、未执行的本机 ARM64 合同或八制品发布标准写作通过。
- 以下消费接入仍由下游 owner 延期，未作为本事项已完成能力或验证结论：推荐流程显示与继续时间由事项 16 负责；设置页日志入口由事项 18 负责；软件优化页最近结果跳转由事项 27 负责；真实调试状态 provider 由事项 32 负责。本事项仅保留已合入的只读历史/日志投影、定位、脱敏诊断导出、日志分段和清空边界。

## Comments

- 2026-08-15：依据已合入的功能树、同一功能 SHA 的 x64 focused 合同和 GitHub x64 CI 完成结票；host ACL/symlink 夹具边界、ARM64 本机延期及四个下游消费接入均明确保留为未验证或延期，未扩大为产品或发布通过。
