# 建立架构与代码质量门禁

Type: task
Status: ready-for-agent
Resolution: completed
Blocked by: 01
Claimed by: Codex issue-31 task
Owner: issue-31
Consumers: 21, 22, 32
Verification: 可重复架构命令、依赖与越层负例、无界面和适配器合同测试、状态所有权与受控扩展审查。
Evidence freshness: 绑定当前提交、项目配置、工具链和模式；代码、依赖或门禁配置变化后重跑。

## Goal

把易维护、低耦合、高内聚、界面与核心分离以及可局部扩展落实为首版实现和发布的硬性门禁，使新增目录内容或内置受控能力时无需改动无关模块，并确保技术先进性不会以牺牲可读性、可测试性和长期维护成本为代价。

## Ownership Boundary

本事项只拥有架构检查框架、无界面测试入口、测试替身接缝和可重复门禁命令。事项 01 的早期只读 CI 调用这些命令，但 `.github/workflows` 的早期构建工作流仍由事项 01 唯一拥有。各业务事项拥有自己的状态机、场景真值表和验收数据，事项 32 拥有调试模式与目录编辑器场景，事项 21 只汇总本事项针对候选提交的门禁结果；本事项不复制这些语义，也不以发布汇总反向定义测试。

## Acceptance Criteria

- [ ] 模块职责、公开接口和单向依赖关系已有明确说明；界面层依赖核心接口，核心通过自己拥有的接口请求平台能力，平台适配器不得反向依赖界面或把平台细节泄漏给可移植核心。
- [ ] 应用只有一个明确的装配入口负责创建并连接核心、目录、持久化和平台适配器；业务模块不得各自隐式创建跨层依赖或使用服务定位器绕过装配入口。
- [ ] 核心状态是任务规划、执行进度、恢复状态和结果的唯一事实来源；界面只提交意图并显示核心状态，不维护能够与核心分歧的第二份业务状态。
- [ ] 自动检查证明调试模式、当前有效软件目录、目录草稿和目录编辑会话分别只有规格指定的单一核心所有者；界面只能提交类型化意图，不能直接解析或写入 TOML、复制目录校验、创建界面私有状态或绕过 owner。具体状态转换与恢复真值表由事项 02、03、18、32 提供。
- [ ] 门禁框架能运行各 owner 提供的正常、失败、恢复和拒绝场景，并检查状态唯一来源、写入边界及结果不被界面或适配器重解释；本事项不维护另一份恢复或目录编辑状态机。
- [ ] 依赖图不存在循环依赖，界面代码不直接修改注册表、配置、进程、文件系统或其他系统状态。
- [ ] 同一业务规则只在一个明确的核心模块或目录契约中定义；界面、平台适配器和多个功能模块不得复制或各自解释同一规则。
- [ ] 不存在跨功能共享的全局可变业务状态；确需跨模块共享的状态必须由职责明确的核心对象拥有，并通过受控接口访问。
- [ ] 可移植核心能够在不启动 WinUI 3、无需管理员权限、无网络且不真实修改电脑的条件下完成自动测试，覆盖规划、状态变化、失败恢复、结果判定和日志语义。
- [ ] 无界面调度合同使用可控测试调度器覆盖单核路径、受限并行上限、取消、异常与结果合并，并证明结果与串行计算具有相同业务结论；并行内部计算不得创建第二份批次状态、改变批次顺序或跨实例互斥，也不得并行启动安装器、系统优化、软件优化、权威提交、结果确认或恢复判断。
- [ ] 无界面测试入口能执行事项 02、03、15、18、32 提供的目录编辑、恢复、日志和诊断场景套件，并允许真实文件、日志、时钟和平台适配器由无副作用替身替换；场景内容和预期业务结果仍由对应 owner 维护。
- [ ] 诊断合同测试入口支持注入目录、来源、下载、安装程序、恢复和异常退出故障，并把事项 15 提供的单文件读取断言作为可重复套件运行；本事项只维护注入与执行框架，不复制诊断字段定义。
- [ ] 状态持久化合同测试注入当前权威状态与最后可信代次同时损坏及事务残留，证明核心只能进入状态损坏恢复模式：所有修改意图被拒绝，原始字节与诊断证据保留，仍可恢复的历史只读可见，明确重新初始化形成新代次且不会把损坏内容标为已恢复。
- [ ] 界面与核心之间、核心与平台适配器之间均有合同测试，覆盖正常结果、失败结果、取消、重启屏障和不受支持状态，确保替换界面或适配器不会改变既有业务语义。
- [ ] 凡既有内置受控能力能够表达的普通软件、下载来源、驱动入口、系统设置或软件优化方案，只增加目录定义及其数据；自动检查会证明既有页面、主流程和无关能力模块无需修改。
- [ ] 新增一种内置受控能力时，改动只涉及对应核心接口、生产与无副作用测试适配器、唯一装配入口、必要的版本化目录模式及相关无界面测试、合同测试和架构检查；未知执行语义或未知能力会使旧版本拒绝整份候选目录并保留当前有效目录，不得修改无关页面、无关目录类型或其他执行能力。
- [ ] 构建和持续检查会自动检测循环依赖、越层依赖、界面直接使用系统操作能力、违反模块边界以及未完成的合同测试；任何违反项都会阻止发布。
- [ ] 架构检查阻止图形化目录编辑器直接链接目录文件或执行适配器，并证明调试模式不能关闭目录校验、日志脱敏或安全边界；本机试用路径不能修改或伪造正式发布门禁结果，也不能把试用目录送入发行制品。
- [ ] 自动检查负责可机械识别的依赖、状态和边界问题，代码评审同时使用状态所有权、删除检验和规则归属检查重复业务规则及跨功能全局可变业务状态；任一硬性缺陷都阻止发布，不得用事项内例外批准绕过，确需改变规则时必须先修改规格和 ADR。
- [ ] 编译器、语言特性、库和框架优先采用满足产品约束的最新稳定方案；实验性或复杂的新技术只有在改善现有目标且不降低可读性、可测试性、工具链稳定性和双架构可构建性时才能进入生产边界。
- [ ] 架构门禁具有可重复执行的项目命令，并在开发者发布证据中明确记录检查结果；“功能暂时可用”不能替代通过架构与代码质量门禁。
- [ ] 本事项提供的门禁命令可以由事项 01 的早期只读 CI 和事项 21 的候选构建重复调用；本事项不另建工作流、不取得 GitHub 写权限，也不拥有事项 34 的发布触发。

## References

- `GOAL-05`
- `SEC-07`
- `TECH-01` 至 `TECH-21`
- `TECH-22` 至 `TECH-34`
- `CAT-50` 至 `CAT-52`
- `DBG-01` 至 `DBG-10`
- `LOG-09` 至 `LOG-13`
- `.scratch/windows-initial-setup-workbench/issues/01-application-foundation.md`
- `.scratch/windows-initial-setup-workbench/spec.md`
- `docs/engineering/architecture-and-code-quality.md`
- `docs/adr/0027-modular-monolith-and-controlled-extension-seams.md`
- `docs/adr/0042-read-only-recovery-after-dual-state-corruption.md`
- `docs/adr/0045-separate-read-only-ci-from-release-permissions.md`

## Comments

- 2026-08-11：实现基于 `codex/v1-integration` 的精确提交 `edaafae0add79022116967f3340a45ff4c5e9897`。范围仅含统一 CMake/CTest 注册接缝、单一依赖规则、CMake/WinUI/source 依赖提取、测试支持目标、门禁负例、`host-guardrails` preset 与工程说明；没有修改 `.github/workflows`，也没有实现事项 02、03、15、18、32 各自拥有的状态机、真值表、目录编辑、恢复、日志或诊断场景。唯一命令 `cmake --workflow --preset host-guardrails` 已完成配置、构建和当前 4/4 CTest：合法项目图、`core.smoke`、反向/精确循环组，以及测试支持/装配绕过组均通过；组内真实负例还确认目标源码与 include 越权、外部目标传递闭环、原始 CTest、WinUI 短名 include、动态编译源和 UI 直接系统副作用按预期拒绝。后续 consumer owner 只需通过普通 CTest 接缝注册其自有套件，本事项不预造占位测试。

- 2026-08-10：Q4/Q5、制品矩阵、机器级安装版与跨类别控制已分别由 ADR-0035、ADR-0029、ADR-0037、ADR-0043 与 ADR-0041 冻结；ADR-0042 的双代损坏故障注入已进入本事项。工程合同已经闭合，本事项改为 `ready-for-agent`；发布门禁仍须由实际实现和可重复检查结果证明，状态变更不表示门禁已经通过。

- 2026-08-09：维护者通过 `grill-with-docs` 确认低耦合、高内聚、前后端分离、局部扩展、无界面核心测试和架构自动检查均为发布硬门槛；最新技术不得以牺牲维护性为代价。

- 2026-08-11：事项 31 在普通 merge 吸收事项 02/24 后完成统一目标/CTest 注册、UI 合同测试库、Windows/基础设施外部依赖登记和 fixture 同步；由 Windows 静态库 `$<LINK_ONLY:...>` 暴露的空依赖解析失败已收敛为独立 `if/elseif` 捕获修复。最终 head `da506374d78e66d42a3e1fed80bc51a33270c789` 通过 host `cmake --workflow --preset host-guardrails` 10/10、`BUILD_TESTING=OFF` 配置与构建、forced-include 定向负例组、`git diff --check` 和 GitNexus 门禁；Windows read-only validation run `31482733757` 的 x64 Release 与 ARM64 Release 均为 SUCCESS。PR #5 已 Squash 合入为 `829282e5c9981a7673fa36cc96e8ceaa04517e56`，据此标记 `Resolution: completed`。该结论是架构、合同与双架构构建证据，不替代各 consumer owner 的业务场景或真实设备验收。
