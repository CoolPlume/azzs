# Windows 初装自动化

本项目定义并组织 Windows 新设备达到初始可用状态所需的操作。用户可观察的行为规则见 [首版规格](.scratch/windows-initial-setup-workbench/spec.md)，本文件只定义项目统一使用的领域语言。

## 范围与产品

**Windows 首装环境**：
运行 Windows 10 22H2 或 Windows 11、刚完成全新安装或尚未投入使用的 OEM 初始设置设备。
_Avoid_: 旧系统, 受管设备, Windows Server

**最低目标版本**：
初装工作台按设计面向的最早 Windows 版本，当前为 Windows 10 22H2；它不是阻止更早版本运行的硬门槛，也不代表经过兼容性测试。
_Avoid_: 最低可运行版本, 兼容性保证

**Apple 风格适配**：
将 Apple 对清晰层级、即时反馈、空间连续、克制材质和用户掌控的设计原则应用到 WinUI 3，同时保留 Windows 用户熟悉的交互习惯。
_Avoid_: macOS 仿制界面, 苹果皮肤

**有效动效偏好**：
界面层合并 Windows 系统动画开关与应用自身进一步减少动效的偏好后得到的单一结果；只有系统允许且应用没有要求进一步减少时，允许播放本项目定义的动效。
_Avoid_: 覆盖系统动画设置, 页面独立动画开关, 动画完成状态

**每显示器 DPI 适配**：
窗口按所在显示器的有效显示缩放重新测量与布局的界面行为；它与 Windows 文本缩放分开处理，且不承载业务状态。
_Avoid_: 单一全局缩放, 文本缩放, 固定物理像素布局

**图形适配器兼容性**：
Windows 选择集成、独立或混合图形适配器后，工作台仍保持相同功能、信息和输入路径，并允许视觉增强回退的能力。
_Avoid_: 驱动硬件概览, 手动选择 GPU, 图形性能保证

**受限并行工作**：
对不产生系统修改、彼此独立且可合并结果的 CPU 密集型内部计算施加上限后的并行处理；它不启动或并行化初始化操作。
_Avoid_: 并行安装, 并行系统优化, 并行软件优化

**标准视图**：
初装工作台默认使用、优先呈现常用任务与必要状态的界面信息层级；它与高级视图提供相同的软件安装项和选择范围。
_Avoid_: 小白模式, 简陋模式

**高级视图**：
用户在应用设置中启用、用于显示更多状态细节、低频选项、参数和手动控制的界面信息层级；它不增加、隐藏或解锁另一组软件安装项，也不解除精选目录或风险控制边界。
_Avoid_: 专业版, 开发者模式

**初装工作台**：
正式产品名称为“Windows 初装工作台”的本机 Windows 图形应用，用户通过它选择并执行初始化操作；`azzs` 只是仓库标识。
_Avoid_: 浏览器工具, 命令行工具, `azzs` 产品名

**产品标识**：
Windows 初装工作台在应用和八个发行制品上使用的正式显示名与简洁设备准备图形；它不决定仓库标识、文件机器标识或发行拓扑。
_Avoid_: `azzs` 品牌, 安装包格式

**初始化操作**：
用户为使 Windows 首装环境达到初始可用状态而主动执行的一项设备变更。
_Avoid_: 任意系统命令, 后台任务

**常驻管理员会话**：
初装工作台从启动到关闭始终以管理员身份运行的一次工作会话。
_Avoid_: 按批次授权, 按操作提权

**状态主体**：
承载用户级选择、确认和可恢复输入的 Windows 用户身份；首版只支持交互登录用户使用自己的拆分管理员令牌，输入其他管理员凭据的场景不进入工作台。
_Avoid_: 进程令牌用户, 任意管理员帐户

**设备级状态**：
存放在稳定设备根、跨发行形态、应用和 Windows 重启保留的产品状态；该词描述生命周期，不表示所有 Windows 用户都能查看或修改。
_Avoid_: 全局无主体状态, 当前进程状态, 临时缓存

**权威提交**：
使一份新代次成为产品事实、可以对用户显示已保存或相应完成结果的持久化转换；恢复检查点和诊断日志不属于权威提交。
_Avoid_: 写入成功, 临时保存, 日志落盘

**恢复检查点**：
为异常退出或断电后尽量找回未保存内容而保存的副本；它不能把未保存内容提升为已保存事实，也不能覆盖已保存草稿或当前有效目录。
_Avoid_: 自动保存草稿, 已保存未应用, 备份成功

**最后可信代次**：
最近一次通过完整性、模式和业务规则检查、可以安全载入的状态代次；它不等同于文件修改时间最新的版本。
_Avoid_: 最新文件, 最近写入版本, 当前草稿

## 发行与资源

**便携版**：
无需系统安装即可启动的初装工作台发行包。
_Avoid_: 无痕版, 临时脚本

**安装版**：
经 Windows 安装后使用的初装工作台发行包。

**机器级安装版**：
程序资产、安装登记、Repair 与普通卸载归整台设备所有，并通过 UAC 完成安装的安装版；用户级偏好和可恢复输入仍按状态主体隔离。
_Avoid_: 用户级安装版, 双作用域安装版

**标准版**：
不携带救援伴随工具或推荐软件离线资源、联网时按需取得安装资源的初装工作台发行版；它仍可离线启动并使用本地能力。
_Avoid_: 在线版, 普通离线版

**断网救援版**：
在标准版基础上携带两个救援伴随工具、面向 Windows 首装环境尚无可用网络时恢复联网的初装工作台发行版。
_Avoid_: 万能驱动版, 第三方工具合集

**超大离线版**：
包含断网救援版全部资源，并额外携带部分基础环境和推荐软件离线资源的初装工作台发行版。
_Avoid_: 离线资源版, 全量软件仓库

**自包含发行包**：
携带初装工作台启动和运行所需全部运行时依赖的发行包。
_Avoid_: 依赖预装版, 第三方软件合集

**发行架构**：
初装工作台发行包面向的处理器架构，当前包括 x64 和 ARM64。
_Avoid_: Windows 系统架构

**离线软件包**：
无需联网即可安装某个推荐软件的本地安装资源及其所需文件。
_Avoid_: 任意安装文件

**候选资源**：
维护者允许纳入软件目录或发行清单评估的资源；当前及以后候选资源都可随包和公开发布，但这不表示其来源、安全、身份或第三方权利已经得到验证。
_Avoid_: 已验证资源, 已授权资源, 已发布资源

**完整安装包**：
由软件厂商提供、已经包含主要安装内容的安装文件；只有安装所需依赖也齐全时，它才可作为离线软件包使用。

**在线安装器**：
启动后仍需从软件厂商网络取得主要安装内容的安装文件。

**软件包缓存**：
工作台下载并保存在设备上的完整安装包、在线安装器或项目离线软件包副本，与超大离线版内置资源和未完成临时文件相互区分。
_Avoid_: 离线包缓存, 永久软件仓库, 下载临时文件

**项目第三方包源**：
项目在 GitHub 上为特定推荐软件维护的备用安装资源来源，它不是软件厂商的官方来源。
_Avoid_: 首选来源, 项目官方软件源

**维护者直供内容**：
由项目维护者直接提供、用于填充教育资源入口的链接、说明和资源；它不包含会在管理员会话中执行的救援伴随工具。
_Avoid_: 用户生成内容, 自动发现内容, 项目审查结论

**开发者发布证据**：
项目内部随一次发行保存的状态记录，分别说明设计与实现、自动化检查和真实设备验证结果；它不属于普通应用界面或面向用户的 GitHub 发布说明。
_Avoid_: 用户发布说明, 应用内状态, 兼容性承诺

**未实机验证**：
开发者发布证据中用于说明本次发行没有真实 Windows 或目标软件验收结果的状态；它既不是“实机验证通过”，也不阻止已经满足实现与自动化检查门槛的发行。
_Avoid_: 实机通过, 验证失败, 用户警告

## 初始化流程

**推荐初始化流程**：
概览页提供的非强制初始化引导，按驱动、系统优化、软件安装、软件优化四个阶段组织。
_Avoid_: 强制向导, 软件优先流程, 三阶段流程

**流程总结**：
推荐初始化流程完成或暂停时呈现的逐项结果汇总。
_Avoid_: 成功提示

**流程记录**：
一次推荐初始化流程的阶段进度与结果记录，与系统优化执行记录、软件安装批次和优化批次相互区分。

**首版必达项**：
维护者已经明确纳入首版、发布前必须具备工作台受控执行和结果确认能力的软件安装阶段偏好、系统设置项或软件优化选项；仅展示说明或要求用户手动修改不算实现。
_Avoid_: 示例优化, 以后支持, 手动方案, 可选实现

**软件安装项**：
可由用户选中并纳入安装批次的目标软件。
_Avoid_: 软件按钮, 任意安装包

**基础软件**：
每台新装 Windows 默认选择的一类软件安装项。
_Avoid_: 强制软件

**普通软件**：
基础软件之外、由用户按需选择的软件安装项。
_Avoid_: 可选基础软件

**需要额外提示的普通软件**：
因安全软件或反作弊影响、商业许可责任或候选资源未经项目验证等情况而需要就地说明、但仍可由用户选择的普通软件；它默认不勾选，不形成高级视图专属的软件清单。
_Avoid_: 高级视图软件, 禁止软件, 已验证软件

**软件目录维护文件**：
项目维护者直接填写或通过调试模式图形化编辑器维护、由工作台进行模式校验并读取的软件目录权威输入，记录产品分类、版本规则、来源用途、原始地址、发行资源安排和说明，但不携带解析脚本或执行代码。
_Avoid_: 软件目录填写表, Markdown 转换表, 任意下载清单, C++ 硬编码表

**未启用软件条目**：
软件目录维护文件中以 `enabled = false` 暂存、不会进入当前有效目录或提供给用户执行的软件条目。
_Avoid_: 已下架软件

**维护目录发布状态**：
软件目录维护文件整体用于继续维护或正式发行的状态，由 `release_state` 表示；它不说明图形化编辑内容是否已经应用。
_Avoid_: 软件条目启用状态

**运行时加载校验**：
判断候选软件目录能否成为当前有效目录、以及哪些局部错误项必须禁用的检查；它不判断该目录能否进入正式发行包。
_Avoid_: 本机运行成功, 来源审查

**正式发布门禁**：
判断软件目录能否随项目正式发行的完整检查；通过运行时加载校验是正式发布通过的必要但不充分条件，本机可以使用某份目录不表示该检查已经通过。
_Avoid_: 本机应用成功, 实机验证

**本机试用目录**：
已经通过运行时加载校验并由用户显式应用、但尚未通过正式发布门禁的当前有效软件目录身份；它不是第三份目录或第二事实来源。
_Avoid_: 可发布目录, 临时界面状态

**软件目录维护指南**：
说明软件目录维护文件填写方法和字段边界的人类文档；工作台不读取它来决定目录内容或安装行为。
_Avoid_: 运行时目录

**调试模式**：
由用户在设置中明确开启的开发诊断状态；它提高本机日志粒度并显示受控的软件目录编辑工具，但不解除目录校验、风险确认或安全边界，也不改变正式发布门禁结果。
_Avoid_: 安全绕过, 任意命令模式

**调试日志**：
调试模式下按最高可用诊断粒度写入的本机结构化日志；一次导出形成可单独传回的自包含诊断文件，使维护者无需访问原测试机即可还原可观察的环境、操作链和失败位置，同时在写入前排除或脱敏凭据及无关个人资料。
_Avoid_: 遥测, 未脱敏原始数据, 普通执行日志

**图形化软件目录编辑器**：
调试模式中用于维护软件目录同一版本化领域模型的 WinUI 3 工具；它通过应用核心校验并产出 TOML 兼容结果，不是界面私有数据库或任意代码入口。
_Avoid_: 普通软件选择页, 任意目录插件, 第二事实来源

**目录编辑草稿**：
图形化软件目录编辑器中尚未应用的候选软件目录状态；它与 `enabled = false` 的未启用软件条目及 `release_state = "draft"` 的维护目录发布状态含义不同。

**恢复的未保存修改**：
工作台在异常退出或电脑断电后尽量找回、但尚未成为已保存草稿的目录编辑内容；恢复后仍属于未保存状态，不会自动保存或应用，也不得覆盖此前已保存的草稿，启动时只通过不阻断的持续入口提示继续编辑。
_Avoid_: 已保存未应用, 自动保存草稿

**应用目录**：
用户明确要求把已保存且通过运行时加载校验的目录编辑草稿替换为当前有效目录的操作；未通过正式发布门禁的内容可以成为本机试用目录，操作只影响后续列表、规划和新批次。
_Avoid_: 保存草稿, 自动加载, 改写批次快照

**软件依赖**：
一个软件安装项正常安装或使用所需的另一个软件安装项关系。
_Avoid_: 安装顺序建议, 软件分类

**软件安装来源**：
单个软件安装项的取得与安装方式。
_Avoid_: 统一下载后端, 下载链接

**来源用途**：
维护者明确声明的一条软件安装来源在产品中的角色，分为首选来源、普通备选来源和项目 GitHub 备用包；它不会因域名、地址形态或跳转目标而改变。
_Avoid_: 来源类型, 自动识别结果, URL 域名

**来源解析方式**：
工作台访问原始地址后自动识别并采用的受控处理路径，例如托管来源、GitHub 发布数据、直接安装文件或普通网页的内置解析能力。
_Avoid_: 维护者选择的地址类型, 任意网页脚本

**来源解析结果**：
工作台在创建安装批次前得到的完整来源事实，包括原始地址与用途、可区分的显示名称、解析方式、版本、实际资源、架构、安装包类型、联网要求和解析上下文。
_Avoid_: 临时下载链接

**受控安装档案**：
由项目维护并通过稳定标识关联某类软件安装器的安装阶段偏好、交互处置、适用版本、受控执行、完成边界、安装后置行为、重启后验证和结果验证事实；软件目录项只引用它，不复制其中规则。
_Avoid_: 任意安装脚本, 用户安装参数

**安装器交互项**：
第三方安装器在安装过程中提出的条款、隐私或附加推广选择；它与安装完成或结果确认是不同事实。
_Avoid_: 任意网页输入

**附加推广**：
第三方安装器明确标识、且不属于目标软件正常安装所必需的附带产品、服务或推广内容；受控安装档案已知且运行时可可靠识别时默认拒绝，无法可靠判断时按安装器交互处置层级回退。
_Avoid_: 必需组件, 软件安装目标, 任意未知选择

**官方安装界面**：
软件厂商提供的安装程序界面，或由该程序打开、用于继续安装、认证或付款的厂商官方网页；其中的账户与个人资料步骤只由用户自行完成。
_Avoid_: 工作台代填表单

**账户与个人资料步骤**：
第三方安装器中要求登录、注册、验证码、付款或填写个人资料的步骤；它一律由用户在官方安装界面完成，工作台不代填、读取、传递或保存内容。

**安装器交互待处理**：
用户取消、关闭或未完成官方安装界面的账户与个人资料步骤后，当前软件项暂停并等待用户重新尝试或停止批次的状态；它不等于安装失败、安装成功或安装结果待确认。
_Avoid_: 自动继续

**受控自动处置**：
工作台按受控安装档案对已知且可可靠识别的条款、隐私或附加推广交互项自动执行预期选择，是安装器交互处置的第一层；附加推广的默认预期选择为拒绝，不包含账户与个人资料步骤。
_Avoid_: 任意界面自动点击, 条款已验证

**工作台交互确认**：
对已知但无法可靠自动处置的条款、隐私或附加推广交互项，工作台展示实际选择及影响并等待用户确认的第二层处置；它不处理账户与个人资料步骤，也不等于安装成功或法律事实已经验证。
_Avoid_: 官方界面交互, 静默同意

**安装后置行为**：
软件安装器首进程结束后仍可能影响安装完成判断的受控后续行为；它由受控安装档案描述，不能仅凭进程退出推断完成。
_Avoid_: 普通退出码, 未观察行为

**可下载文件来源**：
能够向工作台提供具体软件安装文件的来源，例如官网发布页、官方 GitHub Release 或项目第三方包源。
_Avoid_: 浏览器下载

**托管安装来源**：
由 Microsoft Store、`winget` 等外部机制负责取得和安装软件、且不向软件包缓存交付可复用安装文件的来源。

**官网发布页**：
软件厂商维护、公开该软件当前稳定版本和官方下载入口的页面。
_Avoid_: 固定安装包直链, 第三方下载页

**软件版本**：
软件安装项可安装的一个稳定发行版本。
_Avoid_: 工作台版本, 预览版本

**应用正式稳定发行**：
可由工作台一键更新展示的公开工作台发行，不包含 draft、预发布、beta、preview、nightly 或其他测试发行；它不同于软件安装项的稳定版本。
_Avoid_: 手动下载范围, 测试发行

**应用测试发行**：
在 GitHub 上明确标记为预发布、用于 `1.0` 前验证的 `0.x` beta 工作台发行；它不属于应用正式稳定发行，也不进入普通一键更新候选。
_Avoid_: 开发构建, 受支持的应用版本

**稳定版切换**：
用户当前运行测试发行时，主动请求替换为最新匹配应用正式稳定发行的应用更新操作；它不是自动降级，也不包括手动下载。
_Avoid_: 自动回退, 后台更新, 软件安装最新版

**匹配更新制品**：
与当前工作台具有相同发行架构、内容版和便携版或安装版形态的应用正式稳定发行制品；缺少它时工作台不自动换用其他制品，只提供手动下载入口。
_Avoid_: 任意最新资产, 软件包架构回退, 自动切换制品

**更新恢复选择**：
新工作台版本已经启动但无法进入工作台时，由用户在下一次更新恢复界面中选择重试新版本或恢复旧版本的处理；它不等于自动回退或安装成功。
_Avoid_: 自动回退, 手动下载

**更新故障恢复界面**：
更新故障恢复状态下呈现的恢复界面；它列出各次失败的版本和时间、当前仍未恢复的状态，保留本机数据并提供手动下载和诊断资料，不再自动替换或重新启动已经失败的版本。
_Avoid_: 自动重装, 数据重置, 继续自动回退

**更新故障恢复状态**：
新版本重试失败并自动恢复旧版本后，旧版本也无法进入工作台时保留的只读状态；只有某个版本能够正常使用工作台且原有记录仍可见后才结束，下载、导出诊断资料或打开链接都不算恢复。
_Avoid_: 更新成功, 正常工作台, 数据已重置

**已过支持范围的应用版本**：
当前仍可运行、但不属于最新正式稳定发行的工作台版本；工作台应提示用户更新，但不因该提示阻止继续使用。
_Avoid_: 无法运行, 测试发行

**支持的应用版本**：
项目公开说明承诺处理的工作台正式稳定发行范围；首版只支持最新正式稳定发行，测试发行和更早正式稳定发行不在该范围内。
_Avoid_: 可运行版本, 所有历史版本

**软件发行分支**：
同一厂商为不同用途或人群提供的软件产品分支，例如个人版、企业版或国际版；不同分支的版本号不能互相比较来决定最新版。
_Avoid_: 安装来源

**安装最新版**：
选择当前来源可取得的最新稳定软件版本的一项安装命令。
_Avoid_: 自动升级, 使用任意缓存版本

**应用内下载**：
初装工作台保持运行时直接取得软件安装文件并呈现进度的下载过程。
_Avoid_: 浏览器下载, 关闭后继续的后台下载

**来源手动交接**：
工作台无法可靠解析软件来源时，把维护者填写的原始地址交给用户自行查看、下载并在工作台外运行，随后只按既有规则重新检测目标软件；交接记录跨工作台关闭和 Windows 重启保留。
_Avoid_: 任意安装文件导入

**外部安装已识别**：
来源手动交接后，工作台重新检测到目标软件已经存在的状态；它可以满足只要求软件存在的依赖和软件优化发现，但不证明软件由工作台安装，也不证明版本、来源或架构。
_Avoid_: 工作台安装成功, 已验证来源

**暂时跳过**：
用户选择本次不再处理当前项目、且没有声明项目完成的结果状态；对软件而言，它不证明目标软件存在，不能满足软件依赖或进入软件优化发现，并继续保留在未完成列表。
_Avoid_: 已完成, 已取消

**未完成列表**：
在设备上持续保留仍需用户以后处理的软件项目及其重新检测入口的待办列表；它不是安装批次或成功记录，关闭工作台、重启 Windows 或切换发行形态不会清除其中项目。
_Avoid_: 安装队列, 安装历史, 自动重试队列

**交互式安装**：
工作台启动软件厂商的官方安装程序，由用户在该安装界面中完成选择、账户与个人资料步骤和安装；它是安装器交互处置的第三层回退，不等于来源手动交接、安装结果待确认或手动应急处理。
_Avoid_: 浏览器下载

**静默安装**：
工作台依据精选软件目录中明确支持的安装方式运行官方安装程序，无需用户操作厂商安装界面即可完成的软件安装；其中的安装器交互仍只可按受控安装档案自动处置。
_Avoid_: 任意命令执行, 所有软件通用的一键安装

**系统架构**：
当前 Windows 系统运行所面向的处理器架构。

**软件包架构**：
某个软件安装资源所面向的处理器架构。

**架构回退**：
ARM64 Windows 无可用 ARM64 软件包时，改用 x64 软件包的选择。
_Avoid_: 系统架构切换, 自动重装

**架构偏好**：
用户为后续软件包选择设定的 ARM64 与 x64 取舍策略。
_Avoid_: Windows 架构设置, 单次架构改选

**安装批次**：
用户一次选中并按序执行的一组软件安装项。
_Avoid_: 并行安装

**批次快照**：
安装批次或优化批次创建时冻结、供该批次继续执行和追溯的目录与操作上下文。
_Avoid_: 当前目录, 可变执行计划

**停止批次**：
不再启动安装批次中尚未执行项目的正常停止操作。
_Avoid_: 回滚安装

**强制终止**：
用户明确要求立即结束当前安装程序的高风险操作。
_Avoid_: 安全取消

## 驱动与硬件

**硬件检测**：
识别当前设备关键硬件并生成硬件概览的操作，不包含跑分、烤机或其他硬件测试。
_Avoid_: 驱动扫描安装, 硬件测试

**硬件概览**：
用于寻找驱动的 CPU、显卡、主板、网卡和整机 OEM 型号摘要。
_Avoid_: 跑分报告, 完整诊断报告

**断网救援**：
Windows 首装环境没有可用网络时，先说明状态、再优先使用用户提供的本地厂商网卡驱动、最后才由用户选择通用网卡应急工具的恢复联网流程。
_Avoid_: 自动安装万能驱动, 普通联网检测, 静默网络修复

**救援伴随工具**：
用于辅助网络修复或通用网卡驱动救援的独立程序；只有具备项目控制的源码、版本、可复现构建、许可、完整性、最小冒烟测试和进程令牌合同后，才可随断网救援版发布并由工作台启动，否则只能作为外部交接。
_Avoid_: 第三方救援工具, 工作台插件, 工作台内置能力

**未识别**：
硬件检测没有生成可用硬件概览的结果状态。
_Avoid_: 空白结果, 驱动不可用

**驱动获取入口**：
通向厂商驱动资源的入口，不代表初装工作台管理具体驱动包。
_Avoid_: 驱动安装项, 驱动执行器

**驱动交接中**：
初装工作台已启动外部驱动工具、仍等待用户确认处理结果的驱动阶段状态；用户选择“已完成”后会重新读取必要硬件和网络状态，但该确认和重读都不等同于已验证驱动成功。
_Avoid_: 驱动已完成, 自动检测完成

**驱动助手入口**：
安装或启动硬件厂商驱动助手的驱动获取入口。
_Avoid_: 工作台内置驱动安装

**厂商驱动页入口**：
打开项目维护的硬件厂商或 OEM 驱动页面的驱动获取入口。
_Avoid_: 任意搜索结果, 通用下载页

**精选驱动目录**：
项目维护的驱动助手入口与厂商驱动页入口集合。
_Avoid_: 具体驱动包目录, 软件目录

## 精选目录

**目录数据**：
组成精选软件目录、精选驱动目录、精选设置目录和精选软件优化目录的结构化信息。
_Avoid_: 代码内置逻辑, 离线软件包集合

**当前有效目录**：
工作台当前用于呈现可选内容、规划和创建新操作的一类已校验目录状态；当前有效软件目录可以具有本机试用目录身份，但不等同于目录编辑草稿、权威维护文件或既有批次快照。

**内置受控能力**：
随工作台正式发行包编译和发布、由应用核心以受约束参数调用的执行或检测能力；软件厂商的官方安装程序是其受控操作对象或外部程序，不是能力模块或插件。
_Avoid_: 任意脚本, 目标软件安装器

**目录内容扩展**：
只使用工作台已有内置受控能力增加或调整软件、来源、设置、驱动入口或软件优化方案的目录变更；它不携带可执行代码，也不要求改写现有页面或核心流程。
_Avoid_: 功能插件, 任意脚本

**未知执行语义的目录内容**：
旧工作台不能理解、且可能改变目标、来源、依赖、受控能力、执行、检测、恢复、重启或撤回语义的目录内容；它使候选目录不能加载，直到工作台更新。
_Avoid_: 未知展示字段, 局部依赖错误

**受控能力扩展**：
由项目维护、随新版工作台编译和发布的新执行或检测能力，用于承载现有能力无法表达的新操作类型。
_Avoid_: 运行时脚本

**第三方代码插件**：
由工作台发行包之外提供、在运行时装入并获得工作台权限的可执行扩展；项目既定扩展模型不支持这种扩展形态，未来改变必须另立安全模型和 ADR。
_Avoid_: 目录包, 项目内置能力

**精选软件目录**：
项目维护并允许用户选择的软件安装项集合。
_Avoid_: 自定义软件下载, 任意软件源

**精选设置目录**：
项目维护并允许用户选择的系统优化方案与系统设置项集合。
_Avoid_: 自定义系统脚本, 任意注册表修改

**精选软件优化目录**：
项目维护并允许用户选择的软件优化方案集合。
_Avoid_: 任意优化脚本

**软件与驱动目录包**：
承载精选软件目录和精选驱动目录的独立目录发布物。

**系统设置目录包**：
承载精选设置目录、并独立更新和回退的目录发布物。

**软件优化目录包**：
承载精选软件优化目录、并独立更新和回退的目录发布物。

**目录项稳定标识**：
不随显示名称或链接变化而改变、且不得分配给另一目标的目录项身份。
_Avoid_: 显示名称, 可复用编号

**目录包更新**：
用户主动取得并加载同类新目录包的操作。
_Avoid_: 后台静默配置, 应用更新

**调试模式手动导入**：
仅在调试模式中由用户明确选择候选目录并请求加载的维护操作；普通和高级视图不提供该入口，导入仍受同一核心校验与当前有效目录替换规则约束。
_Avoid_: 普通设置导入, 任意文件执行

**目录包回退**：
将一类目录恢复为其上一份可用版本的操作。
_Avoid_: 应用回退, 清空目录

**目录配置错误**：
目录可被解析但其中的身份或依赖关系不符合目录规则的状态。
_Avoid_: 下载失败

**目录不可用**：
当前没有可被初装工作台安全加载的有效目录包的状态。
_Avoid_: 整个工作台不可用, 单项目录错误

## 软件优化

**软件优化方案**：
项目维护、用于调整某个已安装目标软件自身行为或偏好的精选变更方案；目标软件提供的资源管理器右键扩展也归该软件的方案管理。
_Avoid_: 任意优化脚本, 软件卸载, 垃圾清理

**软件优化选项**：
软件优化方案中可由用户单独选择是否在本次执行的一个具体变更。
_Avoid_: 自动优化

**优化批次**：
用户一次选择并按序执行的一组软件优化方案，与软件安装批次和推荐初始化流程相互区分。
_Avoid_: 并行优化

**停止优化**：
允许当前软件优化步骤安全结束、但不再启动优化批次后续步骤的正常停止操作。
_Avoid_: 撤销优化, 强制关闭目标软件

**强制终止优化**：
用户明确要求立即结束当前软件优化步骤的高风险操作，可能留下不完整的软件配置。
_Avoid_: 强制关闭目标软件, 自动恢复

**可优化软件**：
已经安装且当前精选方案中存在适用软件优化方案的目标软件。
_Avoid_: 所有已安装软件, 待安装软件

**暂无可用优化**：
工作台已经识别到目标软件，但当前精选软件优化目录没有可执行方案的状态；已安装软件处于首版声明支持版本且存在首版必达方案时不得使用此状态，缺少方案属于实现缺陷。
_Avoid_: 暂无整机优化, 软件未安装, 方案已下架

**已下架优化方案**：
当前精选软件优化目录不再提供、不能用于新优化批次，但仍由既有批次快照和历史保留的方案。
_Avoid_: 已删除历史, 当前可执行方案

**紧急撤回**：
因项目确认存在严重风险而阻止受影响、尚未开始的软件安装、系统优化和软件优化操作的设备级规则；已开始或已完成的操作结果与历史保留。
_Avoid_: 普通下架, 自动恢复, 删除历史

**紧急撤回优化方案**：
受紧急撤回影响的软件优化方案；它适用紧急撤回对尚未开始步骤的规则，并优先于普通下架和批次快照规则。
_Avoid_: 普通下架, 自动恢复, 删除历史

**紧急撤回通知**：
项目发布、由工作台自动检查且用于阻止严重风险操作的安全信息；它可影响受影响、尚未开始的软件安装、系统优化和软件优化，但不会加载普通目录变更。
_Avoid_: 软件优化目录包更新, 后台静默更新, 使用遥测

**有效紧急撤回信息**：
当前设备依据最近有效紧急撤回通知保存、用于阻止相关尚未开始操作的设备级安全状态。
_Avoid_: 当前软件优化目录, 普通下架

**紧急撤回信息未知**：
设备首次无法取得紧急撤回通知且没有本机有效信息的状态；它持续提示信息缺口，但本身不阻止新的受控操作。
_Avoid_: 检查通过, 已知紧急撤回, 普通离线状态

## 系统优化

**系统优化**：
用于调整 Windows 自身行为的一级产品能力，包含系统优化方案与可独立执行的系统设置项。
_Avoid_: 系统设置页, 软件优化, 全局优化

**系统优化方案**：
由若干已有系统设置项组成的精选选择方案，本身不复制或替代这些设置项的状态、执行和撤销。
_Avoid_: 设置批次, 一键脚本

**推荐总体优化**：
首版面向 Windows 首装环境提供的系统优化方案，用户可在执行前展开并调整其中的系统设置项。
_Avoid_: 强制优化, 自动优化, 全选优化

**系统优化执行记录**：
用户一次执行推荐总体优化或一组系统设置项时保存的选择、快照和逐项结果，与推荐初始化流程、软件安装批次和优化批次相互区分。

**系统设置项**：
由用户主动触发、用于调整 Windows 自身行为的一项精选配置变更；用户界面不以当前用户或整台电脑划分类别。
_Avoid_: 设置按钮, 任意注册表修改

**设置撤销**：
将可逆系统设置项恢复到其最近一次修改前记录值的操作。
_Avoid_: 恢复系统默认值, 重置全部设置

**恢复记录**：
为设置撤销持久保存的设备级原值记录。
_Avoid_: 会话缓存

**设置执行快照**：
系统设置开始执行时冻结的目标、验证、撤销和重启语义。
_Avoid_: 当前设置目录

**已下架设置**：
当前精选设置目录不再提供、但因已有恢复记录而仍需保留撤销语义的设置项。
_Avoid_: 已删除历史, 可再次应用设置

## 结果与恢复

**状态损坏恢复模式**：
当前权威状态与最后可信代次均无法作为有效产品事实载入时，工作台进入的显式不可修改状态；它保留恢复证据，只有用户了解数据影响并明确重新初始化后才结束。
_Avoid_: 自动重置, 空状态启动, 正常工作台

**软件当前状态**：
工作台在某一时刻观察到的目标软件存在与适用状态；它只描述当前事实，不描述哪次安装操作造成该事实。
_Avoid_: 工作台安装成功, 来源证明

**安装操作结果**：
工作台对一次安装尝试记录的结果，与软件当前状态、计划状态和恢复状态分别建模。
_Avoid_: 安装队列

**无适用项**：
推荐流程某阶段当前没有可由用户执行内容、因而可以直接跳过的结果。
_Avoid_: 用户跳过, 执行失败

**已安装**：
执行前检测到目标软件已经存在的状态，不表示该软件是最新版。
_Avoid_: 已更新, 已重装

**安装成功**：
工作台对某次安装尝试已验证达到其批次快照中预期目标的操作结果；它不由单纯检测到软件存在或来源手动交接推导。
_Avoid_: 自动完成

**已生效**：
执行前检测到系统设置已经符合目标值的状态。
_Avoid_: 已执行, 已修改

**已优化**：
软件优化选项当前符合方案目标的状态，不表示一定由工作台修改。
_Avoid_: 已执行, 优化历史, 用户确认

**部分完成**：
总体方案或批次只有部分项目完成、仍须查看逐项结果的汇总状态。
_Avoid_: 已完成, 全部失败, 单项结果

**需要重新优化**：
目标软件版本或状态变化后，原先符合的软件优化目标已经不再满足的状态。
_Avoid_: 自动重新优化

**版本不适用**：
目标软件版本未知或超出软件优化方案声明适用范围的状态。
_Avoid_: 方案已下架, 永久禁止

**可能不适用**：
当前 Windows 版本超出系统设置项的项目已知适用范围；只有该项具备明确恢复规则时，高级视图才可允许用户确认风险后尝试。
_Avoid_: 已确认不兼容, 永久禁止

**未执行**：
用户已选择、但因停止、依赖阻塞或安全规则而尚未开始的初始化步骤结果。
_Avoid_: 执行失败, 用户未选择

**安装失败**：
软件安装项已执行但未达到预期安装状态的结果。
_Avoid_: 整批失败

**优化失败**：
软件优化选项已尝试执行但未达到方案目标的结果，不表示同一方案内已经完成的其他选项被恢复。
_Avoid_: 整批失败, 自动恢复

**优化结果待确认**：
软件优化步骤已经结束，但工作台无法可靠判断目标是否达到、正在等待用户检查并确认的暂停状态。
_Avoid_: 自动确认

**安装结果待确认**：
工作台在安装批次中启动的官方安装程序结束，或安装后置行为无法可靠判断时，尚不能确认目标软件是否达到预期、正在等待用户处理的暂停状态；来源手动交接不使用该状态。

**活动或待恢复初始化操作**：
工作台拥有且正在执行、等待确认、等待重启或待恢复的安装批次、优化批次或系统设置执行；来源手动交接不属于这类操作。
_Avoid_: 只读历史, 后台任务

**来源失效**：
网络可用时，软件来源的原始地址、发布数据、跳转或目标资源已经无效的状态；它既可能在创建安装批次前的解析中发现，也可能在执行批次快照时发生。
_Avoid_: 自动换源, 下载失败

**来源规则待实现**：
软件来源的原始地址仍有效，但当前工作台尚无对应受控解析能力的状态；它表示实现缺口，不表示来源已经失效或暂时异常。
_Avoid_: 未支持来源, 通用解析失败

**暂时无法解析**：
软件来源因超时、限流或其他暂时异常而无法完成解析、稍后可以重新尝试的状态。
_Avoid_: 永久解析失败, 来源已经删除

**没有适用安装包**：
工作台已经取得软件发布内容，但其中没有同时符合软件发行分支、稳定版本和当前系统架构要求的安装包。
_Avoid_: 没有发布内容, 自动选择不匹配包

**安装包信息待确认**：
工作台已经找到候选安装文件，但无法唯一确认其版本、架构或安装包类型，因而拒绝猜测和执行的状态。
_Avoid_: 猜测安装包, 已确认安装包

**用户拒绝架构回退**：
ARM64 用户明确拒绝在本次软件选择中改用 x64 安装包的结果；它不表示系统架构未知或该软件永久不受支持。
_Avoid_: 永久不支持, 自动取消软件

**架构已变化**：
安装批次创建后重新观察到的系统架构与冻结快照不再一致、使旧批次项目不能继续执行的状态。
_Avoid_: 用户临时改选, 系统架构未知

**等待联网**：
缺少网络且没有可用本地资源时，初始化操作尚未执行的状态。
_Avoid_: 永久不支持

**等待重启**：
初始化操作需要 Windows 重启后才能完全生效或完成验证的状态。
_Avoid_: 自动重启, 已完成

**等待资源管理器重启**：
系统设置已经写入，但需要重启 Windows 资源管理器或 Windows 后才能完成生效与验证的状态。
_Avoid_: 设置失败

**已恢复**：
系统设置已经验证恢复到该次修改前记录状态的结果。
_Avoid_: 恢复默认值, 已撤销但未验证

**暂不支持自动处理**：
非首版必达项虽然保留在精选方案说明中，但当前目录版本还没有可靠受控执行规则的状态；该状态不能用于通过首版发布门槛。
_Avoid_: 永久不支持, 静默省略

**手动应急处理**：
工作台已经具备受控执行能力、但本次因运行时异常或尚未识别的新版本无法完成时提供的临时人工交接；它不属于正常执行路径，也不满足首版能力验收。
_Avoid_: 正常手动安装, 首版实现, 自动完成

**重启屏障**：
后续初始化操作必须等待 Windows 重启才能安全执行的暂停点。
_Avoid_: 普通重启提示, 流程结束

**批次恢复**：
Windows 重启后重新载入被重启屏障暂停内容的过程。
_Avoid_: 自动继续执行, 重建批次

**已取消批次**：
用户取消待恢复内容后保留的只读批次历史。
_Avoid_: 已删除批次, 可继续原批次

**已取消流程**：
用户取消待恢复推荐初始化流程后保留的只读流程记录。
_Avoid_: 已删除流程

**目录移除项**：
当前精选软件目录不再提供、但历史仍需识别的软件安装项。
_Avoid_: 已卸载软件, 可从旧来源强制安装

**执行日志**：
在当前设备上保存的初始化操作诊断记录，与恢复记录相互独立。
_Avoid_: 自动上传报告

**操作验证**：
确认初装工作台发起的初始化操作是否达到预期状态的检查。
_Avoid_: 通用 Windows 诊断, 硬件测试

**操作恢复**：
针对初装工作台发起且未完成的初始化操作进行重试或继续的处理。
_Avoid_: 通用系统修复, 自动回滚
